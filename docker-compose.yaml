services:
  mcp-server:
    container_name: mcp_nightscout
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      - NIGHTSCOUT_TIMEOUT=30
